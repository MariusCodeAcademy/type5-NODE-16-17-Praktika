const { sum, multi } = require('./helper');
// 0x754
const a = 200;
const b = 50;

const sumResult = sum(a, b); // 0x754
const mulitlyResult = multi(a, b);

console.log('sumResult ===', sumResult);
console.log('mulitlyResult ===', mulitlyResult);
